// concat() = Combines two or more arrays and returns a new array.

let scores = [23, 56 , 77, 93];

let players = ["mani", "kiran", "pavan"];

let newplayers = scores.concat(players)

console.log(newplayers);